<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="./css/normalize.css">
  <link rel="stylesheet" href="../../assets/stylesheets/style.css">
  <title>TwitterClone-新規投稿</title>
</head>
<body>
  <header>
    <div class="header-left">
      <p>新規投稿</p>
    </div>
  </header>
  <%= form_with(model: @tweet, local: true, url: confirm_tweets_path ) do |form| %>
    <div class="error_msg">
      <% if @tweet.errors.any? %>
        <h2><%= @tweet.errors.count %>件のエラーがあります。</h2>
        <ul>
          <% @tweet.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      <% end %>
    </div>
    <div class="new_content">
      <%= form.text_area :content,maxlength:140,size:"43x5" %>
      <br>
      <%= form.submit "投稿する"%>
    </div>
    <div class="back_index">
      <%= link_to "一覧画面に戻る", tweets_path %>
    </div>
  <% end %>
  <footer class="footer">
  </footer>
</body>
</html>
